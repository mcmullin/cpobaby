<% provide(:title, "Editing Product ##{@product.item_number}" ) %>

<div class="row">
  <div class="col-lg-12">
    <div class="page-header">
      <h2>Editing Product #<%= "#{@product.item_number}" %></h2>
    </div>
  </div>
  <div class="col-lg-5">
    <%= render 'form' %>

    <%= link_to "back to Product ##{@product.item_number}", @product %><br>
    <%= link_to 'back to Products', products_path %>
  </div>
  <div class="col-lg-7 bs-masthead">
    <p class="lead">Just admin-in', huh.</p>
  </div>
</div>
