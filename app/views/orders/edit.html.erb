<% provide(:title, "Editing Order ##{@order.number}") %>

<div class="page-header">
  <h2>Editing Order #<%= "#{@order.number}" %></h2>
</div>

<%= render 'form' %>

<%= link_to "back to Order ##{@order.number}", @order %><br>
<%= link_to 'back to Orders', orders_path %>
